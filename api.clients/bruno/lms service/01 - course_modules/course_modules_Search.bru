meta {
  name: Search course_modules
  type: http
  seq: 3
}

get {
  url: {{BASE_URL}}/course-modules/search?name=Jeanne Kunde
  body: json
  auth: none
}

params:query {
  name: Jeanne Kunde
  ~description: 
  ~durationInMins: 
  ~courseId: 597e9acd-85f6-4e2f-a48b-52bb386669fe
  ~learningPathId: 5f8b1aa0-6cbf-4734-8b7a-6e718578be43
}

headers {
  x-api-key: {{API_KEY}}
  x-api-secret: {{API_SECRET}}
  content-type: application/json
  Authorization: Bearer {{USER_ACCESS_TOKEN}}
}

tests {
    test('Request is successfully', function () {
        expect(res.getStatus()).to.equal(200);
        var jsonRes = res.getBody();
        expect(jsonRes.Status).to.eql('success');
    });
  
    test('Course modules records retrieved successfully', function () {
         var jsonRes = res.getBody();
          expect(jsonRes.Data).to.have.property('Items');
          expect(jsonRes.Data.Items.length).to.be.at.least(0);
    });
    
}
