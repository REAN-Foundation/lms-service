meta {
  name: Update Course content
  type: http
  seq: 4
}

put {
  url: {{BASE_URL}}/course-contents/{{COURSE_CONTENT_ID}}
  body: json
  auth: none
}

headers {
  x-api-key: {{API_KEY}}
  x-api-secret: {{API_SECRET}}
  content-type: application/json
  Authorization: Bearer {{USER_ACCESS_TOKEN}}
}

body:json {
  {
    "CourseId": "{{COURSE_ID}}",
    "CourseModuleId": "{{COURSE_MODULE_ID}}",
    "Title": "Voluptatibus officia ipsam.",
    "Description": "officiis consequatur",
    "ImageUrl": "https://thad.name",
    "DurationInMins": 9967,
    "ContentType": "Url",
    "ResourceLink": "Maiores voluptatibus reprehenderit.",
    "ActionTemplateId": "{{ACTION_TEMPLATE_ID}}",
    "Sequence": 7757
  }
}

tests {
    test('Request is successfully', function () {
        expect(res.getStatus()).to.equal(200);
        var jsonRes = res.getBody();
        expect(jsonRes.Status).to.eql('success');
    });
  
    test('Course contents updated successfully', function () {
        var jsonRes = res.getBody();
  
        expect(jsonRes.Data).to.have.property('id');
        
        expect(jsonRes.Data).to.have.property('CourseId');
        expect(jsonRes.Data).to.have.property('CourseModuleId');
        expect(jsonRes.Data).to.have.property('Title');
        expect(jsonRes.Data).to.have.property('Description');
        expect(jsonRes.Data).to.have.property('ImageUrl');
        expect(jsonRes.Data).to.have.property('DurationInMins');
        expect(jsonRes.Data).to.have.property('ContentType');
        expect(jsonRes.Data).to.have.property('ResourceLink');
        expect(jsonRes.Data).to.have.property('ActionTemplateId');
        expect(jsonRes.Data).to.have.property('Sequence');
        expect(jsonRes.Data).to.have.property('CreatedAt');
        expect(jsonRes.Data).to.have.property('UpdatedAt');
  
        expect(jsonRes.Data.CourseId).equals('{{COURSE_ID}}');
        expect(jsonRes.Data.CourseModuleId).equals('{{COURSE_MODULE_ID}}');
        expect(jsonRes.Data.Title).equals("Voluptatibus officia ipsam.");
        expect(jsonRes.Data.Description).equals("officiis consequatur");
        expect(jsonRes.Data.ImageUrl).equals("https://thad.name");
        expect(jsonRes.Data.DurationInMins).equals(9967);
        expect(jsonRes.Data.ContentType).equals("Url");
        expect(jsonRes.Data.ResourceLink).equals("Maiores voluptatibus reprehenderit.");
        expect(jsonRes.Data.Sequence).equals(7757);
  
    });
    
}
