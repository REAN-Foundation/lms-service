
meta {
  name: Search User learning
  type: http
  seq: 3
}

get {
  url: {{BASE_URL}}/user-learnings/search?userId=5186723d-9d18-4455-6883-f5de1892d49c&actionId=03bed9c9-f976-58fd-cfe5-8c2a6a144f9b&progressStatus=nostrum&percentageCompletion=5992
  body: json
  auth: none
}


headers {
  x-api-key: {{API_KEY}}
  x-api-secret: {{API_SECRET}}
  content-type: application/json
  Authorization: Bearer {{USER_ACCESS_TOKEN}}
}

tests {

    test('Request is successfully', function () {
        expect(res.getStatus()).to.equal(200);
        var jsonRes = res.getBody();
        expect(jsonRes.Status).to.eql('success');
    });

    test('User learnings records retrieved successfully', function () {
         var jsonRes = res.getBody();
          expect(jsonRes.Data).to.have.property('Items');
          expect(jsonRes.Data.Items.length).to.be.at.least(0);
    });
    
}
