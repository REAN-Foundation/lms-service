
meta {
  name: Get Module Progress
  type: http
  seq: 11
}

get {
  url: {{BASE_URL}}/user-learnings/{{USER_ID}}/modules/{{COURSE_MODULE_ID}}/progress
  body: json
  auth: none
}


headers {
  x-api-key: {{API_KEY}}
  x-api-secret: {{API_SECRET}}
  content-type: application/json
  Authorization: Bearer {{USER_ACCESS_TOKEN}}
}

tests {

    test('Request is successfully', function () {
        expect(res.getStatus()).to.equal(200);
        var jsonRes = res.getBody();
        expect(jsonRes.Status).to.eql('success');
    });

    test('Module progress retrieved successfully', function () {
        var jsonRes = res.getBody();
        
        expect(jsonRes.Data).to.have.property('CourseModule');
        expect(jsonRes.Data.CourseModule).to.be.a('number');
        expect(jsonRes.Data.CourseModule).to.be.at.least(0);
        expect(jsonRes.Data.CourseModule).to.be.at.most(1);

    });
    
}


