meta {
  name: Get Course Enrollment by ID
  type: http
  seq: 16
}

get {
  url: {{BASE_URL}}/enrollments/{{COURSE_ENROLLMENT_ID}}
  body: json
  auth: none
}

headers {
  x-api-key: {{API_KEY}}
  x-api-secret: {{API_SECRET}}
  content-type: application/json
  Authorization: Bearer {{USER_ACCESS_TOKEN}}
}

tests {

    test('Request is successful', function () {
        expect(res.getStatus()).to.equal(200);
        var jsonRes = res.getBody();
        expect(jsonRes.Status).to.eql('success');
    });

    test('Course enrollment retrieved with all required fields', function () {
        var jsonRes = res.getBody();

        expect(jsonRes.Data).to.have.property('id');
        expect(jsonRes.Data).to.have.property('UserId');
        expect(jsonRes.Data).to.have.property('CourseId');
        expect(jsonRes.Data).to.have.property('IsActive');
        expect(jsonRes.Data).to.have.property('StartDate');
        expect(jsonRes.Data).to.have.property('ExpectedEndDate');
        expect(jsonRes.Data).to.have.property('EnrolledAt');
        expect(jsonRes.Data).to.have.property('CreatedAt');
        expect(jsonRes.Data).to.have.property('UpdatedAt');
    });

    test('Course enrollment includes course details', function () {
        var jsonRes = res.getBody();
        
        if (jsonRes.Data.Course) {
            expect(jsonRes.Data.Course).to.have.property('id');
            expect(jsonRes.Data.Course).to.have.property('Name');
        }
    });
}

