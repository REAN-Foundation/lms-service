meta {
  name: Get Learning Path Enrollment by ID
  type: http
  seq: 15
}

get {
  url: {{BASE_URL}}/enrollments/{{LEARNING_PATH_ENROLLMENT_ID}}
  body: json
  auth: none
}

headers {
  x-api-key: {{API_KEY}}
  x-api-secret: {{API_SECRET}}
  content-type: application/json
  Authorization: Bearer {{USER_ACCESS_TOKEN}}
}

tests {

    test('Request is successful', function () {
        expect(res.getStatus()).to.equal(200);
        var jsonRes = res.getBody();
        expect(jsonRes.Status).to.eql('success');
    });

    test('Learning path enrollment retrieved with all required fields', function () {
        var jsonRes = res.getBody();

        expect(jsonRes.Data).to.have.property('id');
        expect(jsonRes.Data).to.have.property('UserId');
        expect(jsonRes.Data).to.have.property('LearningPathId');
        expect(jsonRes.Data).to.have.property('IsActive');
        expect(jsonRes.Data).to.have.property('StartDate');
        expect(jsonRes.Data).to.have.property('ExpectedEndDate');
        expect(jsonRes.Data).to.have.property('EnrolledAt');
        expect(jsonRes.Data).to.have.property('CreatedAt');
        expect(jsonRes.Data).to.have.property('UpdatedAt');
    });

    test('Learning path enrollment includes learning path details', function () {
        var jsonRes = res.getBody();
        
        if (jsonRes.Data.LearningPath) {
            expect(jsonRes.Data.LearningPath).to.have.property('id');
            expect(jsonRes.Data.LearningPath).to.have.property('Name');
        }
    });
}

