
meta {
  name: Post learning_paths
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/learning-paths
  body: json
  auth: none
}


headers {
  x-api-key: {{API_KEY}}
  x-api-secret: {{API_SECRET}}
  content-type: application/json
  Authorization: Bearer {{USER_ACCESS_TOKEN}}
}

body:json {
{
    "TenantId": "{{TENANT_ID}}",
    "Name": "Terry Hartmann",
    "Description": "error tempora",
    "ImageUrl": "https://sofia.name",
    "DurationInDays": 6907,
    "PreferenceWeight": 3306,
    "Enabled": false
  }
}

tests {

    try {
        var respData = res.getBody();
        bru.setEnvVar('LEARNING_PATH_ID', respData.Data.id);
    }
    catch (error) {
        console.log(error.message);
    }
    test('Request is successfully', function () {
        expect(res.getStatus()).to.equal(201);
        var jsonRes = res.getBody();
        expect(jsonRes.Status).to.eql('success');
    });

    test('Learning paths created successfully', function () {
        var jsonRes = res.getBody();

        expect(jsonRes.Data).to.have.property('id');
        
        expect(jsonRes.Data).to.have.property('TenantId');
        expect(jsonRes.Data).to.have.property('Name');
        expect(jsonRes.Data).to.have.property('Description');
        expect(jsonRes.Data).to.have.property('ImageUrl');
        expect(jsonRes.Data).to.have.property('DurationInDays');
        expect(jsonRes.Data).to.have.property('PreferenceWeight');
        expect(jsonRes.Data).to.have.property('Enabled');

        expect(jsonRes.Data.Name).equals("Terry Hartmann");
        expect(jsonRes.Data.Description).equals("error tempora");
        expect(jsonRes.Data.ImageUrl).equals("https://sofia.name");
        expect(jsonRes.Data.DurationInDays).equals(6907);
        expect(jsonRes.Data.PreferenceWeight).equals(3306);
        expect(jsonRes.Data.Enabled).equals(false);

    });
    
}
