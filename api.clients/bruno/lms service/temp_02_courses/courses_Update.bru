
meta {
  name: Put courses
  type: http
  seq: 4
}

put {
  url: {{BASE_URL}}/courses/{{COURSE_ID}}
  body: json
  auth: none
}


headers {
  x-api-key: {{API_KEY}}
  x-api-secret: {{API_SECRET}}
  content-type: application/json
  Authorization: Bearer {{USER_ACCESS_TOKEN}}
}

body:json {
{
    "TenantId": "{{TENANT_ID}}",
    "Name": "Martha Tromp",
    "Description": "nostrum deserunt",
    "ImageUrl": "http://monica.org",
    "DurationInDays": 6388
  }
}

tests {

    test('Request is successfully', function () {
        expect(res.getStatus()).to.equal(200);
        var jsonRes = res.getBody();
        expect(jsonRes.Status).to.eql('success');
    });

    test('Courses updated successfully', function () {
        var jsonRes = res.getBody();

        expect(jsonRes.Data).to.have.property('id');
        
        expect(jsonRes.Data).to.have.property('TenantId');
        expect(jsonRes.Data).to.have.property('Name');
        expect(jsonRes.Data).to.have.property('Description');
        expect(jsonRes.Data).to.have.property('ImageUrl');
        expect(jsonRes.Data).to.have.property('DurationInDays');

        expect(jsonRes.Data.Name).equals("Martha Tromp");
        expect(jsonRes.Data.Description).equals("nostrum deserunt");
        expect(jsonRes.Data.ImageUrl).equals("http://monica.org");
        expect(jsonRes.Data.DurationInDays).equals(6388);

    });
    
}
