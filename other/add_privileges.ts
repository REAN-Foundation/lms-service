
    await Privilege.create({ name: 'Certificates.Create' });
    await Privilege.create({ name: 'Certificates.Search' });
    await Privilege.create({ name: 'Certificates.GetById' });
    await Privilege.create({ name: 'Certificates.Update' });
    await Privilege.create({ name: 'Certificates.Delete' });
    arningPathCourses.Delete' });
    
